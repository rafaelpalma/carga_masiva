<div class="row">
  <div class="col-lg-7">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Bls</h3>
      </div>
      <div class="panel-body">
        <table class="table table-hover">
          <thead>
            <tr>
              <th>Código del Bl</th>
              <th>Fecha de atraque</th>
            </tr>
          </thead>
          <tbody>
            <% @bls.each do |bl| %>
              <tr>
                <td><%= bl.codigo %></td>
                <td><%= bl.fecha_atraque %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
  <div class="col-lg-5">
    <h2>Importar Bls</h2>
    <%= form_tag importar_bls_path, multipart: true do %>
      <div class="row">
        <div class="col-lg-8">
          <%= file_field_tag :file %>
        </div>
        <div class="col-lg-4">
          <%= submit_tag "Importar", class: 'btn btn-default' %>
        </div>
      </div>
      
    <% end %>
  </div>
</div>
